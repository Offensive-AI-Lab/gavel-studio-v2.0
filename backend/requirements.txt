fastapi==0.109.0
uvicorn==0.27.0
# Required by FastAPI when any route uses File() / Form() data — the /upload
# endpoint needs it at import time, not just at request time.
python-multipart>=0.0.26
# WebSocket CLIENT for the registry-sync subscriber (services/registry_sync/
# subscriber.py). uvicorn here is the plain build (NOT [standard]), so the
# websockets client lib isn't pulled in transitively — pin it explicitly.
websockets>=12.0
psycopg2-binary==2.9.11
passlib==1.7.4
bcrypt==4.1.2
python-dotenv==1.0.0
python-jose[cryptography]>=3.3.0
pydantic>=2.10.0
litellm>=1.44.0
tiktoken>=0.5.0
sentence-transformers>=3.0.1
transformers>=4.41.0
keybert>=0.8.4
scikit-learn>=1.4.0
# Classifier engine dependencies
torch>=2.0.0
accelerate>=0.26.0
pytorch-ignite>=0.4.12
matplotlib>=3.7.0
pandas>=2.0.0
pillow>=10.0.0
wandb>=0.16.0